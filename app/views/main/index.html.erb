<div class="d-flex align-item-center justify-content-center">
  <h1 id="">Welcome To the Home page of Buffer clone</h1>

  <% if @user%>
    Logged in as <%= @user.email%>
    <%= button_to "Logout", logout_path, method: :delete %>
  <% end%> 
</div>
